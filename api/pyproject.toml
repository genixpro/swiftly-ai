[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
# Start with high-signal correctness rules while the legacy API contract is
# being migrated; style cleanup can be introduced incrementally.
select = ["E9", "F63", "F7", "F82"]
